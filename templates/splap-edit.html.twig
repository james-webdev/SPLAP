{% extends 'base.html.twig' %}

{% block sub_title %}<span class="edit">Edit</span>{% endblock %}

{% block body %}
        <div class="form-wrapper">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit">Save</button>
            {{ form_end(form) }}
        </div>
{% endblock %}