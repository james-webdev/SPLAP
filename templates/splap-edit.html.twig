{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="form-wrapper">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit">Save</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}